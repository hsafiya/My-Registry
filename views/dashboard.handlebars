<div class="box">
  <div class="columns">
    <div class="column is-3">
      <div class="box registry-box">
        <div class="columns">
          <div class="column">
            {{!-- registry name --}}
            <h1 class="is-size-2 has-text-centered">{{items.0.registry.title}}
              <br> <span>Registry</span>
            </h1>
            <div class="has-text-centered my-2">
              {{!-- event date --}}
              <span class="icon-text">
                <span class="icon">
                  <i class="fas fa-calendar-day"></i>
                </span>
                <span> {{items.0.registry.event_date}} </span>
              </span><br>
              {{!-- event address --}}
              <span class="icon-text">
                <span class="icon">
                  <i class="fas fa-map-marker-alt"></i>
                </span>
                <span> {{items.0.registry.address}} </span>
              </span>
            </div>
            {{!-- footer buttons --}}
            <div class="buttons my-2">
              <button class="my-2 button navbar-start btn add-item">Add item</button>
              <div class="navbar-end">
                <button class="my-2 button btn-dashboard">Edit Registry</button>
                <button class="my-2 button btn-dashboard">Delete Registry</button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    {{!-- separator --}}
    <div class="column is-1"></div>
    {{!-- item list --}}
    <div class="column is-8">
      <div class="box" style="margin-top: 100px;">
        <p class="panel-heading is-centered "> Registry List </p>

        {{#if items.length}}
        <table class="table">
          <thead>
            <tr>
              <th>Item Name</th>
              <th>Item Link</th>
              <th></th>
              <th></th>
              <th>Purchase Status</th>

            </tr>
          </thead>
          <tbody>
            {{#each items as |item|}}
            <tr>
              <td>{{item_name}}</td>
              <td><a href="{{item_url}}" target="blank">This link will take you to the item</a></td>
              <td><a href="" class="button is-warning"> <i class="far fa-edit"></i></a> </td>
              <td><a href="" class="button is-danger"><i class="fas fa-minus-circle"></i></a> </td>
              <td>
                {{#if bought}}
                <i class="far fa-check-circle"></i>
                {{else}}
                <i class="	fas fa-spinner fa-pulse"></i>
                {{/if}}
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
        {{/if}}
      </div>
    </div>
  </div>

  <!--modal-->
  <div class="columns">
    <div class="column">
      <div class="modal">
        <div class="modal-background"></div>
        <div class="modal-card">
          <header class="modal-card-head">
            <p class="modal-card-title">Add new item</p>
            <button class="delete" aria-label="close"></button>
          </header>
          <section class="modal-card-body">
            <div class="field">
              <label class="label">Item Name</label>
              <div class="control">
                <input name="item-name" class="input" placeholder="e.g. Gift Card">
              </div>
            </div>

            <div class="field">
              <label class="label">Item URL</label>
              <div class="control">
                <input name="item-url" class="input" placeholder="http://getgifcard.com">
              </div>
            </div>
          </section>
          <footer class="modal-card-foot">
            <button id="add-item" class="button btn">Add item</button>
            <button class="button">Cancel</button>
          </footer>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="/javascript/dashboard.js"></script>
<script src="/javascript/add-item.js"></script>